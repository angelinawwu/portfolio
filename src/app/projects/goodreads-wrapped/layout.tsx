export const metadata = {
  title: 'Goodreads Wrapped ＊ Angelina Wu',
  description: 'A playful reading year summary, delivered to 5000+ users.',
};

export default function GoodreadsWrappedLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
